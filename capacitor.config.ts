import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'simple-todo-ionic-angular',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
